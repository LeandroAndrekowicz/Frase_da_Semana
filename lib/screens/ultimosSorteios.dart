// ignore_for_file: file_names

import 'package:flutter/material.dart';

class UltimosSorteiosScreen extends StatelessWidget {
  const UltimosSorteiosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Últimos Sorteios'),
      ),
      body: const Column(
        children: [
          
        ],
      )
    );

  }
}